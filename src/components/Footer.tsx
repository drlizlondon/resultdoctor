export function Footer() {
  return (
    <footer className="mt-24 border-t border-border/60 bg-card/40">
      <div className="max-w-[1280px] mx-auto px-5 sm:px-8 py-8 text-xs sm:text-sm text-muted-foreground leading-relaxed text-center max-w-3xl">
        ResultDoctor is being built from published NHS clinical guidelines. It is not an NHS
        service, procurement, or local commissioning agreement unless explicitly stated for a
        given pathway. It supports clinical navigation and does not replace clinical judgement.
      </div>
    </footer>
  );
}
